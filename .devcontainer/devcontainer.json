{
    "name": "SumOfSquares dev container",

    "build": {
      "dockerfile": "Dockerfile"
    },

    "onCreateCommand": "echo \"Installing Lean 4 and setting up a project with Mathlib dependency...\" && lake exe cache get! && echo \"Finished setup! Open the README file and follow the instructions there :-)\"",  

    "customizations": {
      "vscode" : {
        "extensions" : [ "leanprover.lean4" ]
      }
    }
  }